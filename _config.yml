name : Hank Quinlan, Horrible Cop
markdown : kramdown
permalink : /blog/:year/:month/:day/:title
include_dir: ./src
defaults :
  - 
    scope:
      path: "" # all files in project
    values:
      layout: "default"