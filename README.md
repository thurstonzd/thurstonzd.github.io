To build the set up the site, including creating the Gemfile, use the following:

```ps1
bundle init
budnle add jekyll
```

To build the site and serve it on  your local machine, use the following:

```ps1
bundle exec jekyll serve
```